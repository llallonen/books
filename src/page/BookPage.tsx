import { Button, Flex, Spinner } from "@chakra-ui/react";
import { useNavigate, useParams } from "react-router-dom";

const BookPage = () => {
  const navigate = useNavigate();
  const book = useParams();

  return <div>BBOKKK PAGE</div>;
};


export default BookPage